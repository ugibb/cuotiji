# PDF 提取
pdfplumber>=0.11.0
pymupdf>=1.24.0          # fitz

# AI 模型 SDK
anthropic>=0.40.0        # Claude
openai>=1.50.0           # GPT-4o（备选）
dashscope>=1.20.0        # Qwen（阿里云）

# DeepSeek 使用 OpenAI 兼容接口，无需额外包

# MathPix
requests>=2.31.0         # MathPix REST API（无官方 SDK）

# 工具
python-dotenv>=1.0.0
Pillow>=10.0.0           # 图片处理
tqdm>=4.66.0             # 进度条
tabulate>=0.9.0          # 对比报告表格输出

# LaTeX / TikZ → SVG（需要系统安装 dvisvgm 或 pdf2svg）
# 仅做 SVG 渲染验证，Python 层只调用 subprocess
